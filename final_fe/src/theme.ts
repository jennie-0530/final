import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#9252e7',
    },
    secondary: {
      main: '#9c27b0',
    },
  },
});

export default theme; // theme 객체를 export
